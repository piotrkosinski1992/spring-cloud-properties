server:
  port: 8762
  
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    healthcheck:
      enabled: true

logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF
